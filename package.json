{
  "name": "tdk-memory-runtime-build",
  "private": true,
  "type": "module",
  "scripts": {
    "build:memory-manifest": "bun build skills/tdk-memory-checksum/scripts/memory-manifest.ts --target=node --format=cjs --outfile skills/tdk-memory-checksum/scripts/memory-manifest.cjs",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "yaml": "2.9.0"
  }
}
